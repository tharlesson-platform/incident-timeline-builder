# Multi Incident

Scenario com dois incidentes no mesmo lote para validar agrupamento automatico.

## Resultado esperado

- pelo menos dois clusters em `timeline.json`
- um incidente principal ligado a `checkout-api`
- eventos de `catalog-api` em um grupo separado
